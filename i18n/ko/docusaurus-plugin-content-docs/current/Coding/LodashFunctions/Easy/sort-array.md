---
id: sort-array
title: 📄 Sort Array
slug: /sort-array
---

## Question Description

숫자로 이루어진 배열이 주어졌을 때, `sort` 함수를 사용하여 배열을 정렬하세요. 다음 두 가지 경우에 대한 해결 방법을 제시하세요:

1. 작은 것부터 큰 것 순으로 정렬(오름차순)
2. 큰 것부터 작은 것 순으로 정렬(내림차순)

### 오름차순 정렬

```js
const numbers = [10, 5, 50, 2, 200];

// 비교 함수 사용
numbers.sort(function (a, b) {
  return a - b;
});

console.log(numbers); // [2, 5, 10, 50, 200]
```

### 내림차순 정렬

```js
const numbers = [10, 5, 50, 2, 200];

numbers.sort(function (a, b) {
  return b - a;
});

console.log(numbers); // [200, 50, 10, 5, 2]
```

### 의도적으로 string 삽입

```js
const mixedNumbers = [10, '5', 50, '2', 200];

mixedNumbers.sort(function (a, b) {
  return Number(a) - Number(b);
});

console.log(mixedNumbers); // ['2', '5', 10, 50, 200]
```

하지만 이 해결 방법은 숫자로 변환할 수 없는 문자열(예: `'iphone'`, `'ipad'`)을 제외할 수 없습니다. 이러한 문자열은 `NaN`으로 변환되며, 정렬 결과에서 마지막에 올 수도 있지만 브라우저에 따라 다른 결과가 나올 수 있습니다. 이런 경우에는 `filter`를 사용하여 먼저 제외한 후 배열을 재구성하는 것을 고려해야 합니다.

### Object 정렬

```js
const mockArray = [
  { type: 'a', label: 1 },
  { type: 'a', label: 2 },
  { type: 'c', label: 1 },
  { type: 'c', label: 3 },
  { type: 'b', label: 2 },
];
```
